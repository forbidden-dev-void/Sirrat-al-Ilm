/// <reference types="vite/client" />

/**
 * Ambient types for the Vite environment.
 *
 * `vite/client` provides the module declarations that let TypeScript accept
 * non-code imports such as `import './styles/tokens.css'` and asset URLs
 * (`import logo from './logo.svg'`). Without this reference the build fails
 * with "Cannot find module './styles/global.css'".
 */
